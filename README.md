# 🚀 TronDNS - نصب خودکار ByoSH DNS Server

یک اسکریپت خودکار برای نصب و پیکربندی ByoSH DNS Server با پشتیبانی کامل از بازی‌های آنلاین فیفا.

## ✨ ویژگی‌ها

- ✅ نصب خودکار ByoSH DNS Server
- ✅ حذف خودکار دامنه‌های EA/FIFA برای بازی آنلاین
- ✅ پیکربندی خودکار iptables
- ✅ تنظیمات Docker
- ✅ پشتیبانی از بازی آنلاین فیفا

## 🎯 نصب سریع (یک خطی)

```bash
bash <(curl -s https://raw.githubusercontent.com/asanseir724/trondns/main/install_byosh.sh)
```

یا:

```bash
wget -qO- https://raw.githubusercontent.com/asanseir724/trondns/main/install_byosh.sh | bash
```

**نکته**: `YOUR_USERNAME` را با نام کاربری گیت‌هاب خود جایگزین کنید.

## 📋 پیش‌نیازها

- سیستم عامل: Ubuntu/Debian
- دسترسی root یا sudo
- اتصال اینترنت

## 🔧 نصب دستی

```bash
git clone https://github.com/asanseir724/trondns.git
cd trondns
chmod +x install_byosh.sh
sudo ./install_byosh.sh
```

**نکته**: `YOUR_USERNAME` را با نام کاربری گیت‌هاب خود جایگزین کنید.

## 📖 نحوه استفاده

1. اسکریپت را اجرا کنید
2. در صورت درخواست، IP عمومی سرور را وارد کنید
3. منتظر بمانید تا نصب کامل شود

## 🎮 تنظیمات DNS برای بازی آنلاین فیفا

برای اتصال آنلاین در فیفا، باید از DNS ترکیبی استفاده کنید:

### در تنظیمات DNS سیستم (کنسول/کامپیوتر):

- **DNS اول**: IP سرور شما (ByoSH)
- **DNS دوم**: `8.8.8.8` (یا `1.1.1.1`)

این تنظیمات باعث می‌شود:
- دامنه‌های در لیست ByoSH → از ByoSH resolve شوند
- دامنه‌های EA/FIFA → از Google DNS resolve شوند

## 🧪 تست DNS

```bash
# تست ByoSH
dig @YOUR_SERVER_IP google.com

# تست EA domains
dig @8.8.8.8 accounts.ea.com
```

## 🔍 چه کارهایی انجام می‌شود؟

1. به‌روزرسانی سیستم و نصب وابستگی‌ها
2. دریافت سورس ByoSH از گیت‌هاب
3. غیرفعال کردن systemd-resolved
4. اصلاح Dockerfile
5. **حذف دامنه‌های EA/FIFA از لیست domain**
6. ساخت ایمیج Docker
7. تنظیم iptables
8. اجرای کانتینر ByoSH
9. دانلود `main.py` در پوشه `~/py-api`

## ⚙️ پورت‌های مورد استفاده

- **53/UDP**: DNS Server
- **80/TCP**: HTTP
- **443/TCP**: HTTPS

## 🛠️ عیب‌یابی

### مشکل: نمی‌توانم آنلاین بازی کنم

**راه حل:**
1. مطمئن شوید که از DNS ترکیبی استفاده می‌کنید (DNS اول: سرور، DNS دوم: 8.8.8.8)
2. بررسی کنید که پورت‌های مورد نیاز باز هستند
3. اطمینان حاصل کنید که دامنه‌های EA/FIFA از لیست domain حذف شده‌اند

### مشکل: خطا در اجرای Docker

```bash
sudo systemctl status docker
sudo systemctl start docker
```

## 📝 نکات مهم

- اسکریپت به صورت خودکار از پوشه `domain` یا `domine` بکاپ می‌گیرد
- دامنه‌های EA/FIFA که حذف می‌شوند:
  - `ea.com`, `fifa.com`, `easports.com`, `origin.com`
  - `*.ea.com`, `*.fifa.com`
  - و سایر زیردامنه‌های مربوطه

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 🤝 مشارکت

مشارکت‌های شما خوشامد است! لطفاً:
1. این ریپازیتوری را Fork کنید
2. شاخه جدیدی ایجاد کنید (`git checkout -b feature/AmazingFeature`)
3. تغییرات را Commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. به شاخه Push کنید (`git push origin feature/AmazingFeature`)
5. یک Pull Request باز کنید

## 📞 پشتیبانی

اگر مشکلی دارید یا سوالی دارید، لطفاً یک Issue باز کنید.

---

**توجه**: این پروژه برای استفاده آموزشی و تحقیقاتی است. استفاده از آن به عهده خود کاربر است.


